# ---- SECRETS. Never commit these.
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# ---- Data (regenerable via analysis/fetch_data.py)
data/
*.csv
*.csv.gz
*.parquet
*.db
*.sqlite
!docs/**/*.csv

# ---- Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# ---- Node / Next.js / Expo
node_modules/
.next/
out/
.expo/
dist/
build/

# ---- Vercel
.vercel

# ---- OS
.DS_Store
Thumbs.db

# Local, per-machine Claude Code approvals — do not track
.claude/settings.local.json

# Local backfill progress state — regenerated per machine
.backfill_progress.json

# Full-res rail icon sources. The served copies in web/public are downscaled to 128px
# (they render at 42-56px); these ~1MB originals stay local so re-exports are possible.
assets/rail-src/
